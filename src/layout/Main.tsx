import MainContent from 'components/MainContent/MainContent'

type Props = {}
const Main = (props: Props) => {
    return (
        <main>
            <MainContent />
        </main>
    )
}
export default Main
